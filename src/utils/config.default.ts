

export const defaultConfig: Config = {
  alias: {},
  title: 'simpler paper',
  docPath: '/',
  output: 'dist',
  theme: 'default',
  miniMap: false,
  backToTop: false,
  indicator: true,
  expandAll: false,
}
