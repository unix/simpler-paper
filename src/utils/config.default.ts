

export const defaultConfig: Config = {
  alias: {},
  title: 'simpler paper',
  docPath: '/',
  output: 'dist',
  theme: 'plain',
  miniMap: false,
  backToTop: false,
  indicator: true,
  expandAll: false,
}
