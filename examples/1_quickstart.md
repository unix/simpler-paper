## QuickStart


### Step 1. Install

run `npm install -g simpler-paper`  


### Step 2 Build
run: 
```bash
# in the project directory run "paper build {dir}"
# ex:
paper build ./docs
```



## QuickStart


### Step 1. Install

run `npm install -g simpler-paper`  


### Step 2 Build
run: 
```bash
# in the project directory run "paper build {dir}"
# ex:
paper build ./docs
```





## QuickStart


### Step 1. Install

run `npm install -g simpler-paper`  


### Step 2 Build
run: 
```bash
# in the project directory run "paper build {dir}"
# ex:
paper build ./docs
```





## QuickStart


### Step 1. Install

run `npm install -g simpler-paper`  


### Step 2 Build
run: 
```bash
# in the project directory run "paper build {dir}"
# ex:
paper build ./docs
```





## QuickStart


### Step 1. Install

run `npm install -g simpler-paper`  


### Step 2 Build
run: 
```bash
# in the project directory run "paper build {dir}"
# ex:
paper build ./docs
```







